# PR113 VPS validation

Code tested: 5f4208c. CI 34752740656 passed repository checks, PostgreSQL integration, browser journeys, production-mode Inbound and dependency audits. AMD64 backend/web images built and passed the release-filter HIGH/CRITICAL scan (ignore unfixed). Native image/PDF/video rendering passed; object storage writes were not exercised. The running application stayed on v0.1.0-rc.6.

A private in-memory run used the real configured Luna provider and the existing KCS evidence and workspace context. It started from briefing and produced a new draft, performed one audit repair, then stopped after six model calls (181,908 ms total model latency). The final status was blocked, not ready: the auditor left the opening claim ungrounded although the critic approved the editorial content. No readiness override, production content write or publication occurred. This proves bounded execution, not successful end-to-end publication.

The blocked draft was rendered separately for visual inspection. Two renderer defects were reproduced: an insight silently omitted the end of a 197-character explanation despite available vertical space, and a comparison column clipped its text horizontally. The corrective delta preserves all insight lines, calculates readable type and panel bounds from the available space, rejects copy that cannot fit, and tightens comparison wrapping. The same frozen draft was inspected after correction; the affected pages showed the full copy. Borderline panel/footer and panel/secondary-copy spacing receive geometry regressions. Manifest versions distinguish the corrected rendering.

Private raw provider results and PDF evidence are retained outside Git. No additional generation is needed to reproduce the rendering defects. The unresolved factual-audit finding remains blocking; these checks do not establish general editorial quality or justify marking the deliverable ready. Final checks and image verification must apply to the corrective commit before deployment.
